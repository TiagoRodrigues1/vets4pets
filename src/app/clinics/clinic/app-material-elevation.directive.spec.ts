import { AppMaterialElevationDirective } from './app-material-elevation.directive';

describe('AppMaterialElevationDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMaterialElevationDirective();
    expect(directive).toBeTruthy();
  });
});
