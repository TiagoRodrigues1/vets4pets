import { PipeVaccinePipe } from './pipe-vaccine.pipe';

describe('PipeVaccinePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeVaccinePipe();
    expect(pipe).toBeTruthy();
  });
});
